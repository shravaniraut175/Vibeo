==> Authentication
    => register
    => login
    => logout (token blacklisting)
    => [OTP based registration]

==> post
    => create
    => can see the feed
    => like posts (collection types)
    => save post

==> users
    => followers
    => following